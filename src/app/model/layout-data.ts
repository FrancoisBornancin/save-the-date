import { CustomImageData } from "./image-data";

export interface LayoutData {
    key: number;
    height: number;
    width: number;
    opacity: number;
    imageBackgroundColor: string;
    imageText: string;
    hasBeenSaved: string;
}
